"""MQTT or WebSocket bridge between devices."""

class DeviceBridge:
    """Placeholder communication layer."""

    def send_command(self, device_id: str, command: str) -> None:
        print(f"Sending '{command}' to {device_id}")
